# Cheirograph Python tooling - pinned for reproducibility.
# Install:  pip install -r requirements.txt
# Versions below are what the Phase 2 plots were generated with (Python 3.14).
numpy==2.2.6
matplotlib==3.10.7
pyserial==3.5        # not used yet; lands with the live plot_raw.py in Phase 4
